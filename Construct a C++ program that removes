Construct a C++ program that removes a specific character from a given string and 
return the updated string. 

#include<iostream>
#include<string>
using namespace std;

int main()
{
    string s;
    string res="";
    char c;

    cout<<"Enter string:\t"<<endl;
    getline(cin,s);
    cout<<"Enter character to remove:"<<endl;
    cin>>c;
    for(int i=0;i<s.size();i++)
    {
        if(s[i]!=c)
        {
            res=res+s[i];
        }
    }
    cout<<"The resulting string is : "<<res<<endl; 
}

